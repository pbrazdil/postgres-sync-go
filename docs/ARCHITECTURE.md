# Architecture Doc Location

The canonical PulseSync codemap lives at the repository root:

```text
ARCHITECTURE.md
```

Keep that file next to `README.md` so new contributors and future agents can find the physical architecture quickly. Use this `docs/` pointer only to preserve the docs index shape.

# Execution Plans

Use this directory for durable plans that future agents need to resume or audit.

- `active/`: plans currently being implemented or requiring follow-up.
- `completed/`: plans that landed and should remain as design history.

Small one-turn plans do not need files here. Create a plan when the work spans multiple slices, changes architecture, or leaves important decisions that should not live only in chat.
